{
  "name": "sigma-sentinel",
  "type": "module",
  "version": "0.1.0",
  "description": "Always-on monitoring for Sigma Computing data models — schema drift, formula integrity, and blast radius",
  "bin": {
    "sigma-sentinel": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/index.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "express": "^5.2.1",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.4"
  }
}
